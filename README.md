full-stack note-taking application allowing users to create, update, and delete notes.

### Must do
- create .env file that consist of:  MONGO_CONNECTION_STRING, PORT, SESSION_SECRET that is random string.
